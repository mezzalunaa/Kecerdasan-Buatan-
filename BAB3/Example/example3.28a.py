# Example 3.28A LazyPredict setup
# Install once in the project virtual environment:
# python -m pip install lazypredict

try:
    import lazypredict
    print('LazyPredict is installed and ready.')
except ImportError:
    print('Install LazyPredict with: python -m pip install lazypredict')
